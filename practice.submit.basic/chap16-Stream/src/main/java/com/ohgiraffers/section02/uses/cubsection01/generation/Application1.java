package com.ohgiraffers.section02.uses.cubsection01.generation;

import java.util.ArrayDeque;
import java.util.Arrays;
import java.util.stream.Stream;

public class Application1 {
    public static void main(String[] args) {
        /* 배열이나 컬렉션을 이용하여 승트림을 생성하는 방법을 이해하고 사용할 수 있다.

        *배열을 스트림으로 생성하기 */
        /*String[] sarr = {"java", "mariadb", "jdbc"}
        Stream<String> strStream1 = Arrays.stream(sarr);
        strStream1.forEach(System.out::println);

        Stream<String> StrStream2 = Arrays.stream(0, 2)*/

    }
}
